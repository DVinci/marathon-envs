[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "marathon-envs"
requires-python = ">=3.10"
dependencies = ["gymnasium>=0.29.1", "mlagents-envs==1.1.0", "numpy>=1.21"]
